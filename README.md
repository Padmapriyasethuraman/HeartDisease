# Bayesian Network for Heart Disease Prediction

Overview

This project demonstrates the use of Bayesian Networks for predicting heart disease based on various health metrics. The implementation uses the `pgmpy` library for modeling and inference.
Dependencies

Ensure you have the following libraries installed:

- `numpy`
- `pandas`
- `pgmpy`

You can install them using pip:

```bash
pip install numpy pandas pgmpy

